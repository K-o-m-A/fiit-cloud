apiVersion: v1
kind: Service
metadata:
  name: {{ include "autoscaler-operator.fullname" . }}
  labels:
    {{- include "autoscaler-operator.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autoscaler-operator.selectorLabels" . | nindent 4 }}
