apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoscaler-operator.fullname" . }}-test-connection"
  labels:
    {{- include "autoscaler-operator.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autoscaler-operator.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
